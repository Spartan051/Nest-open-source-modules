export enum EnMessages {
  DEFAULT_NOT_FOUND = 'One of the desired items was not found',
  NOT_FOUND = 'was not found',
  DEFAULT_FORBIDDEN = 'You do not have access to this section',
  FORBIDDEN = `You do not have access to the (dynamic) section`,
  DEFAULT_BAD_REQUEST = 'There is a problem sending the request',
  INTERNAL_SERVER = 'internal server error',
}
